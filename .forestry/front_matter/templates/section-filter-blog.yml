---
label: 'Section: Filter Blog'
hide_body: true
fields:
- name: categories_list
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Categories
  description: List of categories that blog posts can be tagged with.
